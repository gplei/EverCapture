| Thing          | Convention                   |
| -------------- | ---------------------------- |
| Interface      | PascalCase                   |
| Type Alias     | PascalCase                   |
| Class          | PascalCase                   |
| Enum           | PascalCase                   |
| Variable       | camelCase                    |
| Function       | camelCase                    |
| Constant value | UPPER_SNAKE_CASE (sometimes) |

2026
====
02/27
in stock.* module, create select to allow 'Account' and 'Symbol'.  When 'Account' is selected, show select of accounts with a text box and an button.  The button take care of add/update accounts.  when one of account is selected, create a table of account balance.  you take care of front end only, I will fill up the server side.
